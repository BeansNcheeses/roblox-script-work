local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "Arsenal Chaos Hub | Rayfield",
   LoadingTitle = "Gearing Up...",
   LoadingSubtitle = "by Gemini",
   ConfigurationSaving = {
      Enabled = true,
      FolderName = "ArsenalChaos",
      FileName = "MainConfig"
   },
   KeySystem = false
})

local Settings = {
    Aimbot = false,
    TeamCheck = true,
    FOV = 100,
    ShowFOV = true,
    ESP = false,
    WalkSpeed = 16
}

-- COMBAT TAB
local Combat = Window:CreateTab("Combat", 4483362458)

Combat:CreateToggle({
   Name = "Enable Aimbot",
   CurrentValue = false,
   Flag = "AimToggle",
   Callback = function(Value) Settings.Aimbot = Value end,
})

Combat:CreateToggle({
   Name = "Team Check",
   CurrentValue = true,
   Flag = "TeamToggle",
   Callback = function(Value) Settings.TeamCheck = Value end,
})

Combat:CreateSlider({
   Name = "Aimbot FOV",
   Range = {30, 500},
   Increment = 5,
   CurrentValue = 100,
   Flag = "FOVSlider",
   Callback = function(Value) Settings.FOV = Value end,
})

Combat:CreateSection("Weapon Mods")

Combat:CreateButton({
   Name = "Infinite Ammo & No Recoil",
   Callback = function()
       -- Arsenal Memory Sweep for Ammo/Recoil
       for _, v in pairs(getgc(true)) do
           if type(v) == "table" and rawget(v, "Ammo") then
               v.Ammo = 999
               v.MaxAmmo = 999
               v.Recoil = 0
               v.Spread = 0
           end
       end
       Rayfield:Notify({Title = "Mods Enabled", Content = "Infinite Ammo & No Recoil Active", Duration = 3})
   end,
})

-- VISUALS TAB
local Visuals = Window:CreateTab("Visuals", 4483362458)

Visuals:CreateToggle({
   Name = "Wallhack (Highlight ESP)",
   CurrentValue = false,
   Flag = "ESPToggle",
   Callback = function(Value) 
       Settings.ESP = Value 
       if not Value then
           -- Clean up ESP when toggled off
           for _, p in pairs(game.Players:GetPlayers()) do
               if p.Character and p.Character:FindFirstChild("Highlight") then
                   p.Character.Highlight:Destroy()
               end
           end
       end
   end,
})

Visuals:CreateToggle({
   Name = "Show FOV Circle",
   CurrentValue = true,
   Flag = "ShowFOV",
   Callback = function(Value) Settings.ShowFOV = Value end,
})

-- MOVEMENT TAB
local Movement = Window:CreateTab("Movement", 4483362458)
Movement:CreateSlider({
   Name = "Speed Hack",
   Range = {16, 100},
   Increment = 1,
   CurrentValue = 16,
   Flag = "SpeedSlider",
   Callback = function(Value)
       if game.Players.LocalPlayer.Character then
           game.Players.LocalPlayer.Character.Humanoid.WalkSpeed = Value
       end
   end,
})

-- CORE LOGIC
local FOVCircle = Drawing.new("Circle")
FOVCircle.Thickness = 1.5
FOVCircle.Color = Color3.fromRGB(255, 255, 255)
FOVCircle.Filled = false

local function IsVisible(targetPart)
    local char = game.Players.LocalPlayer.Character
    local cam = workspace.CurrentCamera
    local params = RaycastParams.new()
    params.FilterDescendantsInstances = {char, targetPart.Parent}
    params.FilterType = Enum.RaycastFilterType.Exclude
    local result = workspace:Raycast(cam.CFrame.Position, targetPart.Position - cam.CFrame.Position, params)
    return result == nil
end

game:GetService("RunService").RenderStepped:Connect(function()
    local cam = workspace.CurrentCamera
    local lp = game.Players.LocalPlayer
    local center = Vector2.new(cam.ViewportSize.X/2, cam.ViewportSize.Y/2)
    
    FOVCircle.Visible = Settings.ShowFOV
    FOVCircle.Radius = Settings.FOV
    FOVCircle.Position = center

    -- AIMBOT LOGIC
    if Settings.Aimbot and lp.Character then
        local nearest = nil
        local shortestDist = math.huge

        for _, v in pairs(game.Players:GetPlayers()) do
            if v ~= lp and v.Character and v.Character:FindFirstChild("Head") and v.Character.Humanoid.Health > 0 then
                if Settings.TeamCheck and v.Team == lp.Team then continue end

                local pos, onScreen = cam:WorldToScreenPoint(v.Character.Head.Position)
                if onScreen then
                    local screenDist = (Vector2.new(pos.X, pos.Y) - center).Magnitude
                    if screenDist < Settings.FOV and screenDist < shortestDist then
                        if IsVisible(v.Character.Head) then
                            shortestDist = screenDist
                            nearest = v
                        end
                    end
                end
            end
        end
        if nearest then
            cam.CFrame = CFrame.new(cam.CFrame.Position, nearest.Character.Head.Position)
        end
    end

    -- HIGHLIGHT ESP LOGIC (Fixed for Arsenal)
    if Settings.ESP then
        for _, v in pairs(game.Players:GetPlayers()) do
            if v ~= lp and v.Character and v.Character.Humanoid.Health > 0 then
                if not v.Character:FindFirstChild("Highlight") then
                    local hi = Instance.new("Highlight", v.Character)
                    hi.OutlineTransparency = 0
                    hi.FillTransparency = 0.5
                    -- Red for enemies, Green for team
                    hi.FillColor = (v.Team == lp.Team) and Color3.fromRGB(0, 255, 0) or Color3.fromRGB(255, 0, 0)
                end
            end
        end
    end
end)
