local Rayfield = loadstring(game:HttpGet('https://sirius.menu/rayfield'))()

local Window = Rayfield:CreateWindow({
   Name = "MM2 Mobile Chaos - v10",
   LoadingTitle = "Finalizing Elite Features...",
   LoadingSubtitle = "by Gemini",
   ConfigurationSaving = {Enabled = false},
   KeySystem = false
})

local Settings = {
    RoleESP = false,
    KillAura = false,
    KillAll = false,
    Noclip = false,
    AutoCoin = false
}

-- FUNCTIONS
local function FlingPlayer(target)
    local lp = game.Players.LocalPlayer
    local char = lp.Character
    local hrp = char:FindFirstChild("HumanoidRootPart")
    local oldPos = hrp.CFrame
    
    if target and target.Character and target.Character:FindFirstChild("HumanoidRootPart") then
        -- Teleport to target and Spin
        hrp.CFrame = target.Character.HumanoidRootPart.CFrame
        local bV = Instance.new("BodyAngularVelocity", hrp)
        bV.AngularVelocity = Vector3.new(0, 999999, 0)
        bV.MaxTorque = Vector3.new(0, math.huge, 0)
        
        task.wait(0.5) -- Short burst to avoid map ejection
        bV:Destroy()
        hrp.CFrame = oldPos -- Return to safety
    end
end

-- 1. MAIN TAB
local MainTab = Window:CreateTab("Main", 4483362458)
MainTab:CreateToggle({
   Name = "Role ESP",
   CurrentValue = false,
   Callback = function(V) Settings.RoleESP = V end,
})

MainTab:CreateToggle({
   Name = "Auto-Collect Coins",
   CurrentValue = false,
   Callback = function(V) Settings.AutoCoin = V end,
})

-- 2. MURDERER TAB
local MurderTab = Window:CreateTab("Murderer", 4483362458)
MurderTab:CreateToggle({Name = "Kill Aura", CurrentValue = false, Callback = function(V) Settings.KillAura = V end})
MurderTab:CreateToggle({Name = "Kill All Loop", CurrentValue = false, Callback = function(V) Settings.KillAll = V end})
MurderTab:CreateButton({
    Name = "Trap All Trapdoors",
    Callback = function()
        for _, v in pairs(workspace:GetDescendants()) do
            if v.Name == "Trapdoor" and v:FindFirstChild("ClickDetector") then
                fireclickdetector(v.ClickDetector)
            end
        end
    end
})

-- 3. SHERIFF TAB
local SheriffTab = Window:CreateTab("Sheriff", 4483362458)
SheriffTab:CreateButton({
    Name = "Fling Murderer",
    Callback = function()
        for _, v in pairs(game.Players:GetPlayers()) do
            if v.Backpack:FindFirstChild("Knife") or v.Character:FindFirstChild("Knife") then
                FlingPlayer(v)
            end
        end
    end
})
SheriffTab:CreateButton({Name = "TP to Gun", Callback = function() 
    local gun = workspace:FindFirstChild("GunDrop") or workspace:FindFirstChild("Handle", true)
    if gun and (gun.Name == "GunDrop" or gun.Parent.Name == "GunDrop") then
        game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = gun.CFrame
    end
end})

-- 4. TROLL TAB
local TrollTab = Window:CreateTab("Troll", 4483362458)
TrollTab:CreateButton({
    Name = "Fling Sheriff",
    Callback = function()
        for _, v in pairs(game.Players:GetPlayers()) do
            if v.Backpack:FindFirstChild("Gun") or v.Character:FindFirstChild("Gun") then
                FlingPlayer(v)
            end
        end
    end
})
TrollTab:CreateToggle({Name = "Noclip", CurrentValue = false, Callback = function(V) Settings.Noclip = V end})
TrollTab:CreateButton({
    Name = "Fake Lag",
    Callback = function()
        local char = game.Players.LocalPlayer.Character
        char.HumanoidRootPart.Anchored = true
        task.wait(1)
        char.HumanoidRootPart.Anchored = false
    end
})

-- 5. MISC TAB
local MiscTab = Window:CreateTab("Misc", 4483362458)
MiscTab:CreateButton({Name = "FPS Booster", Callback = function()
    for _, v in pairs(workspace:GetDescendants()) do
        if v:IsA("PostEffect") or v:IsA("Explosion") then v:Destroy() end
    end
end})
MiscTab:CreateButton({Name = "Server Hop", Callback = function() game:GetService("TeleportService"):Teleport(game.PlaceId) end})

-- CORE ENGINE
game:GetService("RunService").Stepped:Connect(function()
    local lp = game.Players.LocalPlayer
    local char = lp.Character
    if not char or not char:FindFirstChild("HumanoidRootPart") then return end

    if Settings.Noclip then
        for _, v in pairs(char:GetDescendants()) do
            if v:IsA("BasePart") then v.CanCollide = false end
        end
    end

    if Settings.RoleESP then
        for _, v in pairs(game.Players:GetPlayers()) do
            if v ~= lp and v.Character then
                local h = v.Character:FindFirstChild("Highlight") or Instance.new("Highlight", v.Character)
                if v.Backpack:FindFirstChild("Knife") or v.Character:FindFirstChild("Knife") then
                    h.FillColor = Color3.fromRGB(255, 0, 0)
                elseif v.Backpack:FindFirstChild("Gun") or v.Character:FindFirstChild("Gun") then
                    h.FillColor = Color3.fromRGB(0, 0, 255)
                else
                    h.FillColor = Color3.fromRGB(255, 255, 255)
                end
            end
        end
    end

    if Settings.AutoCoin then
        local coins = workspace:FindFirstChild("CoinContainer", true)
        if coins then
            for _, coin in pairs(coins:GetChildren()) do
                if coin:IsA("BasePart") then
                    char.HumanoidRootPart.CFrame = coin.CFrame
                    task.wait(0.2)
                end
            end
        end
    end
    
    -- Kill Aura/All Logic
    if Settings.KillAura and char:FindFirstChild("Knife") then
        for _, v in pairs(game.Players:GetPlayers()) do
            if v ~= lp and v.Character and v.Character.Humanoid.Health > 0 then
                if Settings.KillAll then
                    char.HumanoidRootPart.CFrame = v.Character.HumanoidRootPart.CFrame * CFrame.new(0, 3, 0)
                end
                local k = char:FindFirstChild("Knife")
                if k:FindFirstChild("Stab") then k.Stab:FireServer() end
                if Settings.KillAll then break end
            end
        end
    end
end)
